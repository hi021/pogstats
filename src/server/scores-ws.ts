export function saveLastScoreId(scoreId: number) {

}

export function saveScoresBatch(scores) {
const fullLength = scores.length;
// 1. filter
// 2. convert
// 3. save to db
  console.log("saving");
}
